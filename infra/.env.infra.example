# UBL Infrastructure Credentials
# Copy to infra/.env.infra and fill in real values.
# This file is loaded by infra/setup.sh

# ── MinIO (local S3) ─────────────────────────────────────────────
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=ubl
MINIO_SECRET_KEY=ubl-strong-password-2026-LAB512
MINIO_ALIAS=lab512
MINIO_BUCKET=ubl-releases

# ── GitHub (for Actions secrets) ─────────────────────────────────
# Personal access token with repo + actions scope
GITHUB_TOKEN=ghp_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
GITHUB_REPO=danvoulez/fractal-byte

# ── Cloudflare ───────────────────────────────────────────────────
# API token with Zone:DNS:Edit + Tunnel permissions
CF_API_TOKEN=XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
CF_TUNNEL_NAME=ubl-tunnel
CF_DOMAIN=api.ubl.agency

# ── PM2 ──────────────────────────────────────────────────────────
PM2_APP_NAME=ubl-gate
UBL_INSTALL_DIR=/Users/danvoulez/fractal-byte/bin
DEPLOY_POLL_INTERVAL=30
