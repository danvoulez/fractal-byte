# UBL Gate — Environment Configuration
# Copy to .env and adjust for your environment.

# ── Auth ─────────────────────────────────────────────────────────
# Set to "0" to enable auth middleware (default: "1" = disabled in dev)
UBL_AUTH_DISABLED=1

# Dev bearer token (pre-loaded in TokenStore on boot)
UBL_DEV_TOKEN=ubl-dev-token-001

# Comma-separated list of allowed key IDs for the dev token
# Empty = unrestricted (all kids allowed)
UBL_ALLOWED_KIDS=

# ── Server ───────────────────────────────────────────────────────
UBL_BIND=0.0.0.0:3000

# ── S3 (optional, requires --features s3) ────────────────────────
# UBL_S3_BUCKET=ubl-ledger-prod
# UBL_S3_PREFIX=ubl/v1/
# UBL_S3_REGION=us-east-1

# ── Observability ────────────────────────────────────────────────
RUST_LOG=info,ubl_gate=debug
